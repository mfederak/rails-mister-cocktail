<h1><%= @cocktail.name %></h1>

<ul>
  <% @cocktail.doses.each do |dose| %>
  <li><%= dose.ingredient.name %>: <%= dose.description %> (<%= link_to 'Delete a description', dose_path(dose), method: :delete, data: {confirm: "Are you sure?"} %>) </li>
  <% end %>
</ul>

<%= link_to 'Add a description', new_cocktail_dose_path(@cocktail) %>
</br>
<%= link_to 'Back', cocktails_path %>
